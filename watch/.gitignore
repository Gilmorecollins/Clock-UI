.gradle/
build/
local.properties
__pycache__/
*.pyc
.idea/
*.iml
*.apk
*.aab

# Signing material. These must never be committed: keystore.properties holds the
# passwords in PLAIN TEXT, and losing or leaking the .jks means losing the ability
# to ship an in-place update that users can install over the existing app.
*.keystore
*.jks
keystore.properties
